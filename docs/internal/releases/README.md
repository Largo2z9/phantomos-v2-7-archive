# Technical Release History

Schema mutations, breaking changes, and dependency graph updates for contributors integrating new versions of PhantomOS.

Each manifest (`.json`) documents the technical surface area changed in that release: field additions/removals, type changes, migration rules, and compatibility notes.

Audience: template contributors, integrators, and core maintainers. Not for operators.
